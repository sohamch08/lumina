%% ─── Document configuration ────────────────────────────────────────────────
%% Edit these values for each new document. title.tex reads them to build
%% the cover page; \gitrepo overrides the default set in preamble.tex.

% Cover page gradient (top → bottom)
\definecolor{covercolorone}{HTML}{1becff}   % top color
\definecolor{covercolortwo}{HTML}{2E3192}   % bottom color

% TOC & chapter heading accent — drives both the contents page and the chapter title bar
% (overrides the default defined in preamble.tex)
\definecolor{mytoccolor}{HTML}{4549C4}

% Course / document info
\newcommand{\coursetitle}{CSS.201.1 Algorithms}
\newcommand{\courseyear}{TIFR 2024, Aug--Nov}
\newcommand{\instructor}{}              % instructor name, or leave empty

% Scribe / author info
\newcommand{\scribename}{Soham Chatterjee}
\newcommand{\scribeemail}{soham.chatterjee@tifr.res.in}
\newcommand{\scriberwebsite}{https://sohamch08.github.io/}  % leave empty to hide
\newcommand{\scriberwebsitetext}{sohamch08.github.io}

% Git footer — overrides the placeholder set in preamble.tex
\renewcommand{\gitrepo}{sohamch08/lumina}
